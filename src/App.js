import React from "react";
import "./App.css";
import { hot } from "react-hot-loader";

const App = () => {
	return (
		<div>
			<h1>Hello Mars!!!!</h1>
		</div>
	);
};

export default hot(module)(App);
